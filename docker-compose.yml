version: '3.8'

services:
  dashboard:
    build: .
    container_name: bi-dashboard
    ports:
      - "8501:8501"
    volumes:
      # Monta o banco de dados para persistência
      - ./sample_database.db:/app/sample_database.db:ro
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
